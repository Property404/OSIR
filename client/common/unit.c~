#include "ransomlib.h"
#include <string.h>
#include <stdio.h>


#define failure(x) fprintf(stderr,"Failure: %s\n", x);exit(1)
#define error(x) fprintf(stderr, "%Error: %s\n", x)
int main(){
	char* ec;
	unsigned int ecc=encryptSymmetricKey(&ec,1,"SaltLakeCityUtah\0");
	for(unsigned int i=0;i<ecc;i++)
		printf("%c",ec[i]);
	return 0;
	
}
 

 
